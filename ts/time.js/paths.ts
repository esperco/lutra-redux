/*
  Paths for EA/Exec timestats page
*/
import { Path } from "../lib/routing";

const base = "/time";

export const Home = new Path({ base });

// Remaining paths -- TODO whenever we migrate /time over to lutra-redux